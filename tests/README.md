# Tests (Placeholder)

Suggested coverage:

- preprocessing shape/output tests
- board diff / move detection tests
- UCI mapping tests
- serial protocol formatting tests

Quick validation scripts:

- `tests/validate_motion_pipeline.py` (config load + motion plan + mock robot execution)
