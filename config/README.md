# Config

Configuration files for physical setups and runtime paths.

## Current files

- `physical_setup.example.json`
  - Example combined board + robot setup profile used by `src/robot/config.py`

## Planned additions

- runtime paths config (model path, Stockfish path)
- camera config (index, resolution)
- calibration config (board transform, arm offsets)
